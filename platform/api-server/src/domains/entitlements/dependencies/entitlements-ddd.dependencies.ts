/**
 * Entitlements DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: entitlements
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Entitlements Domain Module - Composition root
 *
 * DDD: Composition root for entitlements domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  AccessCredentialRepositoryAdapter,
  ComputeJobRepositoryAdapter,
  EntitlementRepositoryAdapter,
} from "@tidecove/adapters/entitlements";
import { getIdGeneratorService } from "@tidecove/adapters";
import type { AdapterDynamoDBClient } from "@tidecove/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteCancelComputeJob,
  ExecuteGetAccessCredential,
  ExecuteGetComputeJob,
  ExecuteGetEntitlement,
  ExecuteListComputeJobs,
  ExecuteListEntitlements,
  ExecutePurchaseEntitlement,
  ExecuteScheduleComputeJob,
} from "@tidecove/services/entitlements/usecases";
// Ports (interfaces)
import type {
  AccessCredentialRepository,
  ComputeJobRepository,
  EntitlementRepository,
} from "@tidecove/services/entitlements/ports";

export interface EntitlementsDomainModule {
  repos: {
    accessCredentials: AccessCredentialRepository;
    computeJobs: ComputeJobRepository;
    entitlements: EntitlementRepository;
  };
  useCases: {
    accessCredentials: {
      get: ExecuteGetAccessCredential;
    };
    computeJobs: {
      get: ExecuteGetComputeJob;
      list: ExecuteListComputeJobs;
      update: ExecuteCancelComputeJob;
    };
    entitlements: {
      get: ExecuteGetEntitlement;
      list: ExecuteListEntitlements;
    };
  };
}

export function buildEntitlementsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): EntitlementsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      accessCredentials?: AccessCredentialRepository;
      computeJobs?: ComputeJobRepository;
      entitlements?: EntitlementRepository;
    } = {};

    obj.accessCredentials = new AccessCredentialRepositoryAdapter(dynamoClient);
    obj.computeJobs = new ComputeJobRepositoryAdapter(dynamoClient);
    obj.entitlements = new EntitlementRepositoryAdapter(dynamoClient);
    return {
      accessCredentials: obj.accessCredentials!,
      computeJobs: obj.computeJobs!,
      entitlements: obj.entitlements!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    accessCredentials: {
      get: new ExecuteGetAccessCredential(executionContext, idGenerator, repos.accessCredentials),
    },
    computeJobs: {
      get: new ExecuteGetComputeJob(executionContext, idGenerator, repos.computeJobs),
      list: new ExecuteListComputeJobs(executionContext, idGenerator, repos.computeJobs),
      update: new ExecuteCancelComputeJob(executionContext, idGenerator, repos.computeJobs),
    },
    entitlements: {
      get: new ExecuteGetEntitlement(executionContext, idGenerator, repos.entitlements),
      list: new ExecuteListEntitlements(executionContext, idGenerator, repos.entitlements),
    },
  };
  return { repos, useCases: useCases as EntitlementsDomainModule["useCases"] };
}
