/**
 * Marketplace DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: marketplace
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Marketplace Domain Module - Composition root
 *
 * DDD: Composition root for marketplace domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  MarketplaceFrontRepositoryAdapter,
} from "@tidecove/adapters/marketplace";
import { getIdGeneratorService } from "@tidecove/adapters";
import type { AdapterDynamoDBClient } from "@tidecove/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteGetMarketplaceFront,
  ExecuteListMarketplaceFronts,
  ExecuteRegisterMarketplaceFront,
  ExecuteUpdateMarketplaceFront,
} from "@tidecove/services/marketplace/usecases";
// Ports (interfaces)
import type {
  MarketplaceFrontRepository,
} from "@tidecove/services/marketplace/ports";

export interface MarketplaceDomainModule {
  repos: {
    fronts: MarketplaceFrontRepository;
  };
  useCases: {
    fronts: {
      create: ExecuteRegisterMarketplaceFront;
      get: ExecuteGetMarketplaceFront;
      list: ExecuteListMarketplaceFronts;
      update: ExecuteUpdateMarketplaceFront;
    };
  };
}

export function buildMarketplaceDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): MarketplaceDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      fronts?: MarketplaceFrontRepository;
    } = {};

    obj.fronts = new MarketplaceFrontRepositoryAdapter(dynamoClient);
    return {
      fronts: obj.fronts!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    fronts: {
      create: new ExecuteRegisterMarketplaceFront(executionContext, idGenerator, repos.fronts),
      get: new ExecuteGetMarketplaceFront(executionContext, idGenerator, repos.fronts),
      list: new ExecuteListMarketplaceFronts(executionContext, idGenerator, repos.fronts),
      update: new ExecuteUpdateMarketplaceFront(executionContext, idGenerator, repos.fronts),
    },
  };
  return { repos, useCases: useCases as MarketplaceDomainModule["useCases"] };
}
