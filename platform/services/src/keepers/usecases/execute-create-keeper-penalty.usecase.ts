/**
 * ExecuteCreateKeeperPenalty - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: keepers
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteCreateKeeperPenalty Use Case
 *
 * DDD: Application use case for create KeeperPenalty.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { CreateKeeperPenaltyInput, CreateKeeperPenaltyOutput } from "../dto/keeper-penalty.dto";
import type { ExecutionContextService, IdGeneratorService } from "@tidecove/services/_shared/index.js";
import type { KeeperPenaltyRepository } from "../ports";
import { PermissionDeniedError, ValidationError } from "../errors";
import { CanCreate } from "../policies/can-create.policy";
export class ExecuteCreateKeeperPenalty {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly keeperPenalty: KeeperPenaltyRepository
  ) {}

  async execute(input: CreateKeeperPenaltyInput): Promise<CreateKeeperPenaltyOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.kprId();

    // All field names extracted from OpenAPI spec: amount, attestationId, currency, keeperParticipantId, reason
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!input.keeperParticipantId) {
      throw new ValidationError("keeperParticipantId is required");
    }
    if (!input.currency) {
      throw new ValidationError("currency is required");
    }
    if (!input.amount) {
      throw new ValidationError("amount is required");
    }
    if (!input.attestationId) {
      throw new ValidationError("attestationId is required");
    }    // Check policy
    if (!CanCreate.allow({} as any, "")) {
      throw new PermissionDeniedError("Operation not allowed");
    }

    // Generate entity ID in services layer using IdGeneratorService (keepers domain)
    const entityId = this.idGenerator.kprId();

    // Create entity via repository (pass ID and correlation ID for response meta)
    const entity = await this.keeperPenalty.createKeeperPenalty({ ...input, id: entityId, createdByActorId: (input as any).createdByActorId ?? this.context.getUserId(), createdByActorType: (input as any).createdByActorType ?? "human", orgId: this.context.getOrgId(), correlationId } as any);

    return entity as CreateKeeperPenaltyOutput;
  }
}
