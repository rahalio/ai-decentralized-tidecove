/**
 * ExecuteGetComplianceTag - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: compliance
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetComplianceTag Use Case
 *
 * DDD: Application use case for get ComplianceTag.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetComplianceTagInput, GetComplianceTagOutput } from "../dto/compliance-tag.dto";
import type { ExecutionContextService, IdGeneratorService } from "@tidecove/services/_shared/index.js";
import type { ComplianceTagRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetComplianceTag {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly complianceTag: ComplianceTagRepository
  ) {}

  async execute(input: GetComplianceTagInput): Promise<GetComplianceTagOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.cmpId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).tagId) {
      throw new ValidationError("tagId is required");
    }
    const tagId = (input as any).tagId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.complianceTag.getComplianceTag({ tagId: tagId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${tagId}`);
    }

    return entity as GetComplianceTagOutput;
  }
}
