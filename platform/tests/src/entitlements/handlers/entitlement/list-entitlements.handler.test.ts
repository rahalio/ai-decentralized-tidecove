/**
 * ListEntitlements handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: entitlements
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listEntitlements } from "@tidecove/api-server/domains/entitlements/handlers";
import type { EntitlementsDDDDependencies } from "@tidecove/api-server/domains/entitlements";
import { createEntitlement as createEntitlementEntity } from "@tidecove/factories/entitlements";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@tidecove/tests/shared";
import { ENTITLEMENTS_USE_CASES } from "../__factories__/use-cases.js";

describe("ListEntitlements", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: EntitlementsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<EntitlementsDDDDependencies>(
      ENTITLEMENTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createEntitlementEntity({ orgId }),
      createEntitlementEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListEntitlements, "execute").mockResolvedValue(useCaseResult);

    await listEntitlements(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListEntitlements.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
