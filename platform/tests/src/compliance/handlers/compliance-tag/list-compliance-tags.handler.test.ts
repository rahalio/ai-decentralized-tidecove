/**
 * ListComplianceTags handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: compliance
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { listComplianceTags } from "@tidecove/api-server/domains/compliance/handlers";
import type { ComplianceDDDDependencies } from "@tidecove/api-server/domains/compliance";
import { createComplianceTag as createComplianceTagEntity } from "@tidecove/factories/compliance";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@tidecove/tests/shared";
import { COMPLIANCE_USE_CASES } from "../__factories__/use-cases.js";

describe("ListComplianceTags", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ComplianceDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ComplianceDDDDependencies>(
      COMPLIANCE_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with list of entities", async () => {
    const entities = [
      createComplianceTagEntity({ orgId }),
      createComplianceTagEntity({ orgId }),
    ];
    const useCaseResult = {
      data: { items: entities, nextCursor: undefined, prevCursor: undefined },
      meta: { correlationId: "test", timestamp: new Date().toISOString(), pagination: {} }
    };
    
    vi.spyOn(mockDeps.executeListComplianceTags, "execute").mockResolvedValue(useCaseResult);

    await listComplianceTags(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeListComplianceTags.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
