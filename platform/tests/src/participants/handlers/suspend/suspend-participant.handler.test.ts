/**
 * SuspendParticipant handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: participants
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { suspendParticipant } from "@tidecove/api-server/domains/participants/handlers";
import type { ParticipantsDDDDependencies } from "@tidecove/api-server/domains/participants";
import { createSuspend as createSuspendEntity } from "@tidecove/factories/participants";
import { createCreateSuspendRequest } from "@tidecove/factories/participants";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@tidecove/tests/shared";
import { PARTICIPANTS_USE_CASES } from "../__factories__/use-cases.js";

describe("SuspendParticipant", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ParticipantsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ParticipantsDDDDependencies>(
      PARTICIPANTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = createCreateSuspendRequest();
    const entity = createSuspendEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeSuspendParticipant, "execute").mockResolvedValue(useCaseResult);

    await suspendParticipant(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeSuspendParticipant.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
